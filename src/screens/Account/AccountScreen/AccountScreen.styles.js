import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    indicatorStyle: {
        backgroundColor: '#fff'
    },
    containerTabStyle: {
        backgroundColor: 'transparent'
    }
});